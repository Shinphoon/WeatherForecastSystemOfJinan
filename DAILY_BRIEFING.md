# 每日天气早报

登录成功后后台生成早报；保持登录的用户每天首次打开网站时也会触发。以北京时间、用户 ID 和日期去重，重复登录或并发请求只认领一次邮件发送。天气数据暂不可用时，打开网页后会重试生成。

在「我的」保存所在地、绑定 Gmail 或 QQ 收件邮箱，并开启「天气预警与早报推送」。早报使用账号保存的经纬度，不使用用户临时浏览的国家站。未保存位置时不生成猜测位置的早报。关闭推送仅停止早报邮件，站内消息仍生成。

早报使用已有 Open-Meteo 七日预报接口中的当天数据。站内保留历史，页面展示最近 30 份；早报已读状态按账号存储，支持单条和一键已读。

## 邮件配置

现有 `GMAIL_SENDER_EMAIL` / `GMAIL_APP_PASSWORD` 配置可继续用于 Gmail 发件。Gmail 或 QQ 均可作为收件邮箱。也可以在项目 `.env` 中设置以下变量替代早报发件配置：

```dotenv
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_SENDER_EMAIL=your-account@qq.com
SMTP_PASSWORD=your-smtp-authorization-code
```

465 使用 TLS，其他端口使用 STARTTLS。Gmail 发件默认 `smtp.gmail.com:587`。不要将授权码提交到版本控制。雷达预警现有发件配置不受此改动影响。

## 存储与启动

重启 FastAPI 后端以加载新接口。无需修改已有 SQL Server 表；早报自动存放在 `src/backend/daily_briefings.sqlite3`，已加入 Git 忽略。部署时须持久化并备份此文件，可通过 `BRIEFING_DB_PATH` 指定同一服务实例组共享的本地文件路径。多台服务器部署应改用共享数据库，不应各自持有独立 SQLite 文件。

站点归属统一维护在 `src/data/station-cities.json`，前后端共用。已收录站号优先使用该表，避免旧接口或数据库中的空值、错误城市覆盖正确归属。表中包含截图里的 143 个站点，以及截图间隙中的汶上、宁阳、巨野，共 146 个站点、16 个地级市。未知的新站号使用接口城市，无城市才归入「其他站点」。执行 `node --test src/data/stationCities.test.js` 检查覆盖范围及淄博八站归属。

SMTP 报错或进程在发送途中退出时，消息中心保留早报，显示发送失败或待确认。此类发送不自动重试，避免邮件已被服务器接受但回执丢失时重复发送。管理员应结合发件记录排查。生成和邮件失败均不阻塞登录结果。

离线检查：`python src/backend/test_daily_briefing.py`、`npm run build`。测试替换天气、数据库和邮件依赖，不向真实邮箱发送邮件。

## 临近降雨通知

登录成功后异步检查已保存的用户位置，不使用当前浏览的国家站位置。页面每分钟同步消息，后台沿用每5分钟检查；同账号检查间隔至少5分钟，通知冷却60分钟，SQLite事务防止并发重复发送。

仅最近3帧雷达数据均在30分钟内、当前位置尚未进入回波（最近距离大于2公里）、回波正在接近且预计1至60分钟到达时，生成“您所在的位置即将出现降雨”。已下雨、不接近、数据不足、瓦片失败或数据过期均不推送。该结果是固定位置的雷达趋势估算，不是对用户移动路线的预测；无覆盖地区不能可靠提醒。

通知写入原BRIEFING_DB_PATH数据库的rain_notifications表，自动建表；所有账号可以收到站内消息。已绑定邮箱且开启邮件推送时使用现有SMTP设置投递，支持Gmail/QQ收件邮箱。发送失败保留站内通知并显示状态，不自动重试不确定的SMTP投递。已读状态按账号保存。

离线验证：`src/backend/venv/Scripts/python.exe -m unittest discover -s src/backend -p test_rain_notifications.py`。测试使用临时数据库与模拟雷达/邮件，不发送真实邮件。

## 早晚报告时间与邮件修复（2026-09-18）

“我的 → 邮箱与早晚报告”可保存个人早报与晚报时间，服务端再次校验：05:00—11:00、16:30—22:30（含端点、北京时间）。默认07:00/18:00。新后台任务每60秒检查有效账号，按各自所在地生成早报（当天）和晚报（明日）；不要求用户在线，但必须保持后端服务运行。到点后在对应时间窗内补发，超过窗口不补发。每日每类一次，修改已发送报告的时间不会重复投递。没有邮箱或关闭邮件推送时仍保存站内报告。

复用原briefings表，以日期:morning/日期:evening区分；新增report_schedule表，旧早报及已读状态保留。邮件错误原因保存在email_error字段，界面显示具体类别。SMTP发信成功仅表示服务器接受，不等于已进入收件箱。

本机诊断：Gmail 587端口等待服务器问候超时；465端口SSL连接及现有账号认证成功，统一发送路径已改为465。诊断未发送真实邮件。旧降雨提醒已失去时效，不自动重发。未来投递仍失败时查看消息详情中的错误原因及后端日志。

管理中心不再收取额外管理密码，文章更新/国家站新增仍通过Bearer登录令牌及管理员角色校验。反馈增加reply、replied_at字段，管理员保存回复后用户可在“意见反馈 → 我的反馈”查看。

## 三时段报告与多地点（2026-09-20）

天气报告扩展为早间、午间、晚间三个独立开关，默认时间为07:00、15:20、18:00。服务端分别校验05:00—11:00、11:00—16:30、16:30—22:30（北京时间）；关闭某个开关后，该时段不生成站内消息，也不发送邮件。早、午报告使用当天预报，晚间报告使用次日预报。

首页顶部支持每个账号保存最多5个地点。可通过山东省内地点搜索或浏览器当前位置添加，服务端使用山东省GeoJSON真实边界复核；账号之间数据隔离。切换地点会更新账号当前所在地和最近国家站，因此首页天气、雷达以及后续定时报告均使用新地点。数据保存在`saved_locations.sqlite3`，部署时应与早报数据库一起持久化和备份。
