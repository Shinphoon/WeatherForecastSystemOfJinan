# 山东省气象插值地图

首页与实况页共用 `StationMap.vue`。图层选择支持实时气温、相对湿度、1小时降水、24小时降水、24小时变温、24小时变压、雷达回波和仅边界。站点开关只控制国家站标记，不隐藏定位蓝点、边界或插值图层。

省界来自 [阿里云 DataV 山东省 GeoJSON](https://geo.datav.aliyun.com/areas_v3/bound/370000.json)，本地文件 `src/data/shandong.geojson` 包含 MultiPolygon 岛屿。边界和站点转换到 EPSG:3857 显示，插值按省界裁剪，不对省外着色。底图为 OpenStreetMap。

观测来源为项目现有 q-weather.info 站点实时、今日和历史页面。后台从数据库读取所有启用站点，每组最多8个采集线程；所有客户端共享每组5分钟的进程内缓存。首次请求立即返回进度，页面每5秒取得一次部分结果，完成后每分钟检查。变温、变压历史数据仅在选择相应图层时采集。后端多进程部署会各有一份缓存；当前实现适用于单进程服务。

每个气象要素保留自己的观测时次。排除无时间、非数值、异常范围、超过3小时的观测；同一图层参与插值的时次距离最新报告不超过1小时。缺测不会补零，零降水是有效值。降水没有有效数据时显示空白，不代表无雨。

变温与变压使用最近完整整点的观测减去前一天**完全相同整点**的观测。上游历史数据只有逐小时值，不能当作当前分钟对比；界面因此明确标注“最近完整整点”。缺少当天或昨天对应值时，该站不参与此项插值。

绘制采用 Web Worker 内的 IDW（距离平方倒数加权），每个栅格使用附近最多12站，至少3个有效邻站，距离上限为 Web Mercator 投影190公里（山东纬度约150公里地面距离）。省界内缺少覆盖的区域保留透明。分辨率为240像素宽的省域栅格；插值只反映站点数据的平滑空间估计，不能表达未被站点捕获的局地强对流。色标为分段阈值色标，超出端点沿用端点颜色，原始数值不修改。

气温色标 -20～45℃，相对湿度 0～100%，降水分别使用适合1小时与24小时的色阶；变温、变压以0为中性色分界。图层下方显示实际观测时次、有效站数、采集进度及缺测说明。

检查：`node --test src/map/weatherInterpolation.test.js` 和 `src/backend/venv/Scripts/python.exe src/backend/test_station_fields.py`。反馈持久化在 `src/backend/feedback.sqlite3`（可通过 `FEEDBACK_DB_PATH` 指定），部署需保留此文件。

## 淄博实况产品

实况页使用用户保存的经纬度与 `src/data/ziboBoundary.json` 市界判断是否位于淄博，市界来源阿里云 DataV（370300）。淄博境内展示淄博产品，不随浏览的国家站改变；无定位时保留原有站点城市展示规则。

淄博产品包含降水、气温、风、能见度四类13项，无相对湿度。按北京时间动态生成文件名，08/20时以来产品取最近已过去的对应起点，支持跨日。最多回查12小时，显示实际图片时段。上游为用户提供的 wxzibo2.zbszyqxt.cn，固定来源后端代理解决HTTP混合内容，检查PNG签名、限制5MB、缓存5分钟。

通知优先使用现有 GMAIL_SENDER_EMAIL / GMAIL_APP_PASSWORD，通过 smtp.gmail.com:465 发信；收件地址支持 Gmail、QQ、126、163 等邮箱，不需要收件人提供SMTP密码。界面邮箱提示已同步更新。邮件到达仍取决于收件服务商投递与垃圾邮件过滤；离线测试不发送真实邮件。
